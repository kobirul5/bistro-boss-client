import { Helmet } from "react-helmet-async";

const Menu = () => {
    return (
        <div>
            <Helmet>
                <title>Menu || Bistro Boss</title>
            </Helmet>

        </div>
    );
};

export default Menu;